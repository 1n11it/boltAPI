# =====================================================================
# BOLT API - ENVIRONMENT VARIABLES TEMPLATE
# =====================================================================
# WARNING: This is just a 'template'. Never push your REAL
# passwords, database URLs, or secret keys to GitHub by saving them in this file!
#
# HOW TO USE:
# 1. Make a copy of this file and name it: '.env'
# 2. Fill in your actual system credentials and keys in the '.env' file.
# 3. Ensure that '.env' is already added to your '.gitignore' file.
# =====================================================================

# --- DATABASE CONFIGURATION (PostgreSQL) ---
# These variables help SQLAlchemy/SQLModel connect to the database
DATABASE_HOSTNAME=localhost
DATABASE_PORT=5432
DATABASE_PASSWORD=your_database_password
DATABASE_NAME=boltapi
DATABASE_USERNAME=postgres

# --- AUTHENTICATION & SECURITY (JWT Config) ---
# SECRET_KEY should be a long, random string.
# To generate it, run this in the terminal: openssl rand -hex 32
SECRET_KEY=your_random_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=45